encoder: 
	g++ -O2 -o out/encoder encoder.cpp `Magick++-config --cppflags --cxxflags --ldflags --libs`

decoder: 
	g++ -O2 -o out/decoder decoder.cpp `Magick++-config --cppflags --cxxflags --ldflags --libs`

all: encoder decoder